# 自定义 RustDesk 编译和部署说明

## 📋 已配置的预设参数

### 自建服务器配置
- **服务器 IP**: 39.97.50.6
- **服务器秘钥**: c77HeYQce1GVUCnx2YSyrYxJ1f5GYcyrvUufE8r0toU=
- **Relay 服务器**: 39.97.50.6
- **API 服务器**: http://39.97.50.6

### 固定密码
- **远程管理密码**: 28b5hD8S26
- **临时密码**: 已禁用
- **验证方式**: 仅使用固定密码

### 无人值守模式
- **访问模式**: 完全控制 (full)
- **授权模式**: 密码验证后自动授权
- **键盘/剪贴板/文件传输**: 全部启用
- **音频传输**: 已启用
- **硬件编码**: 已启用
- **自动更新检查**: 已禁用

---

## 🔨 编译步骤

### Windows 平台编译

#### 方法1: 使用 Python 构建脚本（推荐）

```powershell
# 1. 确保已安装依赖
# - Visual Studio 2019/2022 with C++ 工具链
# - Python 3.x
# - Rust (rustup)

# 2. 编译（带预设配置）
python build.py --release --features default_config

# 或编译便携版
python build.py --portable --features default_config
```

#### 方法2: 直接使用 Cargo

```powershell
# 编译 release 版本（带预设配置）
cargo build --release --features default_config

# 编译结果位置
# target\release\rustdesk.exe
```

#### 方法3: 编译 Flutter 版本

```powershell
# 编译 Flutter GUI 版本
cargo build --release --features flutter,default_config

# 或使用完整构建
python build.py --flutter --features default_config
```

### 编译选项说明

| Feature | 说明 | 是否必须 |
|---------|------|----------|
| `default_config` | 启用预设配置（服务器+密码+无人值守） | ✅ 必须 |
| `flutter` | Flutter GUI 界面 | 🟡 可选 |
| `hwcodec` | 硬件编解码 | 🟡 推荐 |
| `inline` | 内联模式 | ⚪ 可选 |

---

## 📦 部署步骤

### 1. 编译完成后的文件

编译成功后，你将得到：
```
target/release/
  ├── rustdesk.exe          # 主程序
  ├── librustdesk.dll       # 库文件
  └── ...其他依赖文件
```

### 2. 打包部署

```powershell
# 创建部署目录
New-Item -ItemType Directory -Path "RustDesk-Deploy"

# 复制可执行文件
Copy-Item "target\release\rustdesk.exe" -Destination "RustDesk-Deploy\"

# 复制必要的 DLL 文件（如果有）
# 通常 Rust 编译的程序已经静态链接，不需要额外 DLL
```

### 3. 首次运行配置

```powershell
# 进入部署目录
cd RustDesk-Deploy

# 安装为系统服务（推荐）
.\rustdesk.exe --install-service

# 或直接运行
.\rustdesk.exe
```

### 4. 验证配置

启动后，检查配置是否生效：

```powershell
# 查看日志确认
# 应该看到以下日志：
# ✅ 已配置自建服务器: 39.97.50.6
# ✅ 已设置固定密码
# ✅ 已启用完全静默模式 (无人值守)
# ✅ 无人值守模式配置完成
```

---

## 🧪 测试验证

### 本地测试

1. **启动被控端**
   ```powershell
   .\rustdesk.exe
   ```

2. **检查配置**
   - 查看 RustDesk ID（自动从服务器获取）
   - 确认固定密码为：28b5hD8S26
   - 确认连接到服务器：39.97.50.6

3. **测试连接**
   - 使用另一台机器的 RustDesk
   - 输入被控端 ID
   - 输入密码：28b5hD8S26
   - 应该无需点击确认，直接连接成功

### 预期效果

| 测试项 | 预期结果 |
|--------|----------|
| 服务器连接 | ✅ 自动连接到 39.97.50.6 |
| ID 获取 | ✅ 从自建服务器获取 ID |
| 密码验证 | ✅ 使用固定密码 28b5hD8S26 |
| 连接授权 | ✅ 无弹窗，自动授权 |
| CM 窗口 | ✅ 不弹出 Connection Manager |
| 功能权限 | ✅ 键盘/鼠标/文件/剪贴板全部可用 |

---

## ⚙️ 其他预设配置建议

### 已包含的推荐配置

当前已包含以下优化配置：

1. ✅ **性能优化**
   - 硬件编码已启用
   - 图像质量设置为 balanced

2. ✅ **安全配置**
   - 禁用自动更新检查（避免未授权更新）
   - 固定密码模式（避免临时密码变化）

3. ✅ **无人值守**
   - 完全静默模式
   - 自动授权连接

### 可选的额外配置

如果需要更严格的控制，可以在 `src/default_config.rs` 中添加：

#### 1. 禁用局域网发现（强制使用服务器）

```rust
// 在 set_recommended_defaults() 函数中添加
Config::set_option("enable-lan-discovery".to_string(), "N".to_string());
```

#### 2. 禁用直接 IP 连接（强制中继）

```rust
// 在 set_recommended_defaults() 函数中添加
Config::set_option("direct-server".to_string(), "N".to_string());
```

#### 3. 设置默认图像质量

```rust
// 可选：low, balanced, best
Config::set_option("image-quality".to_string(), "best".to_string());
```

#### 4. 禁用某些权限

```rust
// 如果不需要文件传输
Config::set_option("enable-file-transfer".to_string(), "N".to_string());

// 如果不需要音频
Config::set_option("enable-audio".to_string(), "N".to_string());

// 如果不需要剪贴板同步
Config::set_option("enable-clipboard".to_string(), "N".to_string());
```

#### 5. 启用会话录制

```rust
// 自动录制所有会话（用于审计）
Config::set_option("enable-record-session".to_string(), "Y".to_string());
Config::set_option("allow-auto-record-incoming".to_string(), "Y".to_string());
```

#### 6. 设置连接超时

```rust
// 设置自动断开超时（分钟）
Config::set_option("allow-auto-disconnect".to_string(), "Y".to_string());
Config::set_option("auto-disconnect-timeout".to_string(), "60".to_string()); // 60分钟
```

---

## 🔒 安全建议

### 当前配置的安全级别

**适用场景**: 
- ✅ 企业内网环境
- ✅ 受信任的办公网络
- ✅ VPN 保护的网络

**不适用场景**:
- ⚠️ 直接暴露在公网
- ⚠️ 未加密的公共网络

### 提升安全性的建议

1. **网络层面**
   - 使用防火墙限制访问 IP
   - 部署在 VPN 或内网环境
   - 定期更换固定密码

2. **配置层面**
   - 启用 2FA（如果支持）
   - 启用会话录制和审计
   - 限制同时连接数

3. **系统层面**
   - 定期更新 RustDesk 版本
   - 监控异常连接日志
   - 使用强密码策略

---

## 📝 常见问题

### Q1: 编译失败怎么办？

```powershell
# 清理缓存后重新编译
cargo clean
cargo build --release --features default_config
```

### Q2: 如何修改预设配置？

编辑 `src/default_config.rs` 文件，修改相应的配置值，然后重新编译。

### Q3: 如何验证配置是否生效？

查看 RustDesk 启动日志，应该包含：
```
✅ 已配置自建服务器: 39.97.50.6
✅ 已设置固定密码
✅ 已启用完全静默模式 (无人值守)
```

### Q4: 如何更改密码？

修改 `src/default_config.rs` 中的密码：
```rust
Config::set_password("你的新密码");
```
然后重新编译。

### Q5: 如何禁用预设配置？

编译时不使用 `--features default_config` 即可：
```powershell
cargo build --release
```

---

## 🚀 快速部署命令

```powershell
# 1. 编译
cargo build --release --features default_config

# 2. 复制到部署目录
Copy-Item "target\release\rustdesk.exe" -Destination "C:\Program Files\RustDesk\"

# 3. 安装服务
cd "C:\Program Files\RustDesk"
.\rustdesk.exe --install-service

# 4. 启动服务
Start-Service rustdesk

# 完成！现在可以使用 ID 和密码 28b5hD8S26 进行连接
```

---

## 📞 支持

如有问题，请检查：
1. 编译日志是否有错误
2. 服务器 39.97.50.6 是否可访问
3. 防火墙是否允许 RustDesk 通信
4. 日志文件中的详细信息

预设配置已完成，现在可以编译和部署了！
